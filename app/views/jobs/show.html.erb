<h2><%= @job.title %></h2>
<span class="featured-job"><%= "Vaga em Destaque!" if @job.featured %></span>
<span class="expired-job"><%= "Vaga expirada!" if @job.expired? %></span>
<hr>
<span class="response"><%= notice %></span>
<div class="job-details">
  <p><b>Localização: </b><%= @job.location %></p>
  <p><b>Empresa: </b><%= @job.company.name %></p>
  <p><b>Descrição: </b><%= @job.description %></p>
  <p><b>Categoria: </b><%= @job.category.name %></p>
</div>
<div class="links">
    <%= link_to 'Voltar', root_path %>
    <%= link_to 'Editar', edit_job_path(@job) %>
</div>
